http:
  routers:
    nextcloud:
      rule: "Host(`cloud.queerreferat.ac`)"
      service: nextcloud

  services:
    nextcloud:
      loadBalancer:
        servers:
          - url: "http://localhost:11000"
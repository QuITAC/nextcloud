http:
  routers:
    nextcloud:
      rule: "Host(`cloud.queerreferat.ac`)"
      entrypoints:
        - "https"
      service: nextcloud

  services:
    nextcloud:
      loadBalancer:
        servers:
          - url: "http://localhost:11000"